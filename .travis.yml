language: node_js
node_js:
 - "lts/*"
cache:
 directories:
   - ~/.npm
   - ~/.cache


   
jobs:
 include:
   - stage: tests
     script:
       - docker run --rm -v $(pwd):/src backstopjs/backstopjs test

language: node_js
node_js:
 - "lts/*"
cache:
 directories:
   - ~/.npm
   - ~/.cache


   
jobs:
 include:
   - stage: tests
     script:
        
        - docker run backstopjs/backstopjs -v tests/backstop:/src test /bin/sh -c "cd /root/test; -f bundle exec npm run backstop:test;"
        - docker ps -a
